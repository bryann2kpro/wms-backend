meta {
  name: Login Account
  type: http
  seq: 1
}

post {
  url: {{local_path}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@mail.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
