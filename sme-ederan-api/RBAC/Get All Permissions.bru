meta {
  name: Get All Permissions
  type: http
  seq: 8
}

get {
  url: {{local_path}}/rbac/permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  ~permissionId: 
  ~moduleId: 
  ~permissionType: 
  ~status: active
}

settings {
  encodeUrl: true
}